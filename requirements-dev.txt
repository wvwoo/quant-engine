-r requirements.txt

# --- test + determinism toolchain (baseline was 0 tests, pytest not even installed) ---
pytest
pytest-socket        # network OFF inside tests; a golden fixture must never hit the wire
time-machine         # frozen clock; maintained C-level alternative to freezegun

# --- static gates ---
mypy
ruff

# --- supply chain ---
pip-audit

# --- coverage gate: a repo floor AND a per-file floor (ci.sh). Added after
# --- api.py/live.py/run_backtest.py were found sitting at exactly 0.0%.
coverage
httpx                # required by fastapi.testclient.TestClient
